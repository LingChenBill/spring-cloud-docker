server:
  port: 8040
spring:
  application:
    name: micro-gateway-zuul
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
# 开启Zuul的路由端点.
management:
  security:
    enabled: false
